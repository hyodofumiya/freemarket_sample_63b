-# =render partial: "/form_header"
%main.content_area.content_area--form
  .form_area
    %h2.form_area__title#form_area_title ユーザー情報編集
    = form_with(model:@user, class: "form_fields form_fields--user", id: "user_form" , local: true) do |f|
      = render partial: "form/user_form", locals: {form: f, user: @user, isrequest_new: false}
    -# = render "devise/shared/links"
-# =render partial: "/form_footer"