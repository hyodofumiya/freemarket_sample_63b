-# =render partial: "/form_header"
%main.content_area.content_area--form
  .form_area
    %h2.form_area__title ユーザー情報編集
    = form_with(model:@user, class: "form_fields form_fields--user", local: true) do |f|
      = render "devise/shared/error_messages", resource: resource
      = render partial: "users/user_form", locals: {f: f}
    -# = render "devise/shared/links"
-# =render partial: "/form_footer"